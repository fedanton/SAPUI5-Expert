_schema-version: "3.2"
ID: vinvoices
version: 0.0.1
modules:
- name: vinvoices-approuter
  type: approuter.nodejs
  path: vinvoices-approuter
  requires:
  - name: vinvoices_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: vinvoices_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: vinvoices_html_repo_host
    parameters:
      content-target: true
resources:
- name: vinvoices_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: vinvoices_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
